import 'package:flutter/material.dart';

class AppColorSchemes {
  static final lightScheme = ColorScheme.fromSeed(
    seedColor: const Color(0XFF553FFB),
  );

  static final darkScheme =
      ColorScheme.fromSeed(seedColor: const Color(0xFF04011C));
}
